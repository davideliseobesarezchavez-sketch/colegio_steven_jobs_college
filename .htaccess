# 🔒 Deshabilitar listado de directorios
Options -Indexes

# 🏠 Forzar que se cargue login.php como página principal
DirectoryIndex login.php

# 🔁 Activar motor de reescritura
RewriteEngine On

# --- 1️⃣ Redirigir cualquier acceso directo al raíz hacia dashboard.php ---
RewriteCond %{REQUEST_URI} ^/$
RewriteRule ^.*$ login.php [L]

# --- 2️⃣ Redirección limpia: quitar .php de las URLs ---
RewriteCond %{THE_REQUEST} \s/+([^.]+)\.php [NC]
RewriteRule ^ %1 [R=301,L]

# --- 3️⃣ Permitir que las URLs sin .php funcionen ---
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{DOCUMENT_ROOT}/$1.php -f
RewriteRule ^(.+?)/?$ $1.php [L,QSA]
