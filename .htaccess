# Deshabilitar listado de directorios
Options -Indexes

# Forzar que siempre se cargue dashboard.php
DirectoryIndex dashboard.php

# Redirigir cualquier acceso directo al directorio raíz hacia dashboard.php
RewriteEngine On
RewriteCond %{REQUEST_URI} ^/$
RewriteRule ^.*$ dashboard.php [L]
