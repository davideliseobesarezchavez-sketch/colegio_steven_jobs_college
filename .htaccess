# ======================================================
# 🚀 SISTEMA ESCOLAR - URLs limpias y seguras (sin .php)
# ======================================================

RewriteEngine On
RewriteBase /colegio_steven_jobs_college/

# ================================
# 🔐 Redirigir raíz al login
# ================================
RewriteCond %{REQUEST_URI} ^/colegio_steven_jobs_college/?$ [NC]
RewriteRule ^$ login [L,R=302]

# =======================================================
# 🧩 Eliminar extensión .php en la raíz
# (Ejemplo: /login → /login.php, /registro → /registro.php, /recuperar_contrasena → /recuperar_contrasena.php)
# =======================================================
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
# Solo letras normales, números, guiones y guiones bajos (sin acentos ni ñ)
RewriteRule ^([a-zA-Z0-9_-]+)/?$ $1.php [L,QSA,NC]

# =======================================================
# ✨ Rutas limpias dentro de /index/
# (Ejemplo: /index/index_admin → /index/index_admin.php)
# =======================================================
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^index/([a-zA-Z0-9_-]+)/?$ index/$1.php [L,QSA,NC]

# =======================================================
# ✨ Rutas limpias dentro de /includes/
# (Ejemplo: /includes/actualizar_tarea → /includes/actualizar_tarea.php)
# =======================================================
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^includes/([a-zA-Z0-9_-]+)/?$ includes/$1.php [L,QSA,NC]

# =======================================================
# 🛡️ Proteger carpetas internas
# =======================================================
RewriteRule ^(PHPMailer-master|vendor|config)/ - [F,L]

# =======================================================
# 🚫 Bloquear archivos sensibles
# =======================================================
<FilesMatch "(^\.|config\.php|conexion\.php)">
  Require all denied
</FilesMatch>

# =======================================================
# 🌐 Evitar listado de directorios
# =======================================================
Options -Indexes

# =======================================================
# ✍️ Forzar codificación UTF-8
# =======================================================
AddDefaultCharset utf-8
